[core]
    remote = minio
['remote "minio"']
    url = s3://dvc-store
    endpointurl = http://minio:9000
    use_ssl = false
    region = eu-central-1
    access_key_id = user
    secret_access_key = password
